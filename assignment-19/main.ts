// store a value in variable

let guests : string [] = ["Muneeb","Ablaj","Zawyar","Usman"]

// number of guests
console.log(`The number of guest list is ${guests.length}`);

// mesage 
console.log(`And the name is that`);

// guests names
guests.forEach(elem => {
    console.log(elem)    
});
